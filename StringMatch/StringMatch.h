#ifndef STRING_MATCH_H
#define STRING_MATCH_H

#include <string>
#include <vector>

class StringMatch
{
public:
	StringMatch();
	~StringMatch();

	//virtual std::vector<int> match(std::string& p, std::string& t);
};

#endif